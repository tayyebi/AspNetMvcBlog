@model IEnumerable<v_Group>
@{
    ViewBag.Title = "مدیریت گروه ها";
}
@Html.ActionLink("گروه جدید", "Create")
<table>
    <tr>
        <th>
            عنوان
        </th>
        <th></th>
    </tr>
@foreach (var item in Model)
{
    <tr>
        <td>@item.Name</td>
        <td>
            @Html.ActionLink("ویرایش", "Edit/" + item.Id)
            @Html.ActionLink("حذف", "Delete/" + item.Id)
        </td>
    </tr>
}
</table>